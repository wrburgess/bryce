File symlinks are now followed without ensuring their targets belong to the checked bundle, making parity validation environment-dependent.

Review comment:

- [P2] Reject Markdown symlinks that escape the bundle root — /private/tmp/bryce-issue-91/scripts/parity-check.ts:540-544
  When a tracked `.md` symlink targets an absolute path or `../` path outside the bundle, this follows and later reads that ambient external file. The parity result can therefore vary between checkouts (or process an arbitrarily large host file) rather than validating only shipped documentation; resolve the target and require it to remain beneath `root` before accepting it as a file alias.
